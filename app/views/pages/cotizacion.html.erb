<h1 class="black-shadow">Tu cotizacion</h1>
<div class="container">
    <div class="d-flex flex-column" style="height: 200px;">
      <div class="p-2">
        <table class="table table-striped table-dark table-hover text-center align-middle">
          <thead class="thead-dark">
            <tr>
              <th scope="col">Distancia(km)</th>
              <th scope="col">Price per km($/km)</th>
              <th scope="col">Cotización estimada($)</th>
              <th scope="col">
                <h4><i class="fas fa-arrow-down"></i></h4>
              </th>
              </th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td scope="row"><%= @distancia.round(1)  %></td>
              <td><%= @promedio.round(1) %></td>
              <td>
                <h4>
                  <span class="badge badge-pill badge-warning"><%= @cotizacion.round(1) %></span>
                </h4>
              </td>
              <td>
                <form method="get" action="/loads/new">
                    <button type="submit" class="btn btn-primary">Publicar carga</button>
                </form>
              </td>
            </tr>
          </tbody>
        </table>
    </div>
</div>

